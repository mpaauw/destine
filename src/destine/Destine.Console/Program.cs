using System;
using System.IO;

namespace Destine
{
    public class Program
    {
        static void Main(string[] args)
        {
            // wait for input:
            while(Console.Read)

            // once input received, parse it
            //  -> validate input, run commands if applicable
            //  -> error handling / user input validation

            // continue to wait for input once commands are done
        }
    }
}
